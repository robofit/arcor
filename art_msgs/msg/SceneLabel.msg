# Text (e.g. work instruction) / image displayed on the table / spatially to the human

geometry_msgs/PoseStamped pose # top left corner of the label
float64 width
float64 height
string text
string image_filename
